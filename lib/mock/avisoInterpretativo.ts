/**
 * Texto fixo usado como `aviso_interpretativo` nos mocks (doc 03, §1.1). Em
 * produção este texto vem da resposta da API, nunca hard-coded no
 * componente — aqui ele só precisa existir uma vez para os vários mocks
 * (T1, T2, T3) devolverem o mesmo valor.
 */
export const AVISO_INTERPRETATIVO =
  "Este escore mede atipicidade estatística do perfil disciplinar do atleta, e não probabilidade de fraude. A finalidade é priorizar atenção humana.";
