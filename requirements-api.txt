# Backend da POC (src/api). O pipeline de dados usa pandas, pyarrow e scikit-learn, já presentes no ambiente.
fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0
httpx>=0.27  # TestClient dos testes
# psycopg[binary]>=3.2  # apenas se ANALISE_BETS_DATABASE_URL apontar para PostgreSQL
